export enum Action {
  JOIN = "join",
  JOINED = "joined",
  DISCONNECTED = "disconnected",
  CODE_CHANGE = "code-change",
  SYNC_CODE = "sync-code",
  LANG_CHANGE = "lang-change",
  SYNC_LANG = "sync-lang",
}
